# ボタン・十字キー・スティックの値

`S` コマンド（`S <btn> <hat> <lx> <ly> <rx> <ry>`）の各項目の値です。
いずれも 16 進数で指定します。

## ボタン（btn）

各ボタンにビットが割り当てられています。複数ボタンを同時に押す場合は
値を足し合わせます（例：A＋B＝`0x4`＋`0x2`＝`0x6`）。

有線モードのビット配置は無線の S 行と同じ 16 ボタンです。
SL/SR/Capture まで送れます。

| 値 | ボタン |
| ---- | ---- |
| `0x1` | Y |
| `0x2` | B |
| `0x4` | A |
| `0x8` | X |
| `0x10` | L |
| `0x20` | R |
| `0x40` | ZL |
| `0x80` | ZR |
| `0x100` | MINUS（−） |
| `0x200` | PLUS（＋） |
| `0x400` | L スティック押し込み |
| `0x800` | R スティック押し込み |
| `0x1000` | HOME |
| `0x2000` | CAPTURE |
| `0x4000` | SL（Joy-Con 横持ち用。プロコンでは無視） |
| `0x8000` | SR（Joy-Con 横持ち用。プロコンでは無視） |

## Joy-Con（L）の十字キー

Joy-Con（L）に十字キーはありません。hat の値が上下左右の押下に
読み替えられます（`T 1` のときだけ）。Joy-Con（R）では hat は
無視されます。

## 十字キー（hat）

| 値 | 向き |
| ---- | ---- |
| `0` | 上 |
| `1` | 右上 |
| `2` | 右 |
| `3` | 右下 |
| `4` | 下 |
| `5` | 左下 |
| `6` | 左 |
| `7` | 左上 |
| `8` | 中立（押していない） |

## スティック（lx ly rx ry）

`0x00`〜`0xFF` の範囲で指定します。`0x80` が中立です。上下は上が小さく・
下が大きくなる方向です（例：左スティック上＝`ly` を `0x00` 寄りに）。

## 入力例

```
S 4 8 80 80 80 80       ← A ボタンのみ
S 6 8 80 80 80 80       ← A＋B（0x4＋0x2）
S 0 0 80 80 80 80       ← 十字キー上（ボタンなし）
S 0 8 80 0 80 80        ← 左スティック上（ボタンなし）
S 1000 8 80 80 80 80    ← HOME
N                       ← 全解放
```
